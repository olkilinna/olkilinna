---
const { columns = '3' } = Astro.props

const baseClasses = 'grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-6 list-none p-0 my-10 items-start'
const desktopCols = columns === '2' ? 'lg:grid-cols-2 mx-auto' : 'lg:grid-cols-3'
---

<ul class:list={[baseClasses, desktopCols]}>
  <slot />
</ul>
